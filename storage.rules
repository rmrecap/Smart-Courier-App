rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{country}/users/{userId}/proofs/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
      allow delete: if request.auth.token.admin == true;
    }
  }
}
